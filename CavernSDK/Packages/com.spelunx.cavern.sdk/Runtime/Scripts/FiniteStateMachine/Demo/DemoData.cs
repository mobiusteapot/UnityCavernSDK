using UnityEngine;

// Use a MonoBehaviour to store data that might be shared by multiple states, or just data in general.
public class DemoData : MonoBehaviour {
    public int healthPoints = 100;
    public int manaPoints = 100;
}